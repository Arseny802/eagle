set(PROJECT_NAME fsmanager)

file(GLOB SOURCE_FILES_C *.c)
file(GLOB HEADER_FILES_C *.h)
file(GLOB SOURCE_FILES_CPP *.cpp)
file(GLOB HEADER_FILES_CPP *.hpp)

add_library(
    ${PROJECT_NAME}
	STATIC
    ${SOURCE_FILES_C}
	${SOURCE_FILES_CPP}
    ${HEADER_FILES_C}
	${HEADER_FILES_CPP}
)